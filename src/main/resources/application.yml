spring:
  application:
    name: "challange"
  datasource:
    url: jdbc:h2:mem:outsera_challange
    driverClassName: org.h2.Driver
    username: root
    password: root
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
